{
  "scripts": {
    "build": "gulp build",
    "build-debug": "gulp build -d",
    "start": "http-server web",
    "deploy": "aws s3 sync --profile=s3 --delete --metadata-directive REPLACE --cache-control max-age=1800,public web s3://readaloud.news"
  },
  "devDependencies": {
    "del": "^2.0.2",
    "gulp": "^3.9.0",
    "gulp-concat": "^2.6.0",
    "gulp-if": "^2.0.0",
    "gulp-newer": "^1.0.0",
    "gulp-print": "^2.0.1",
    "gulp-uglify": "^1.4.2",
    "yargs": "^3.31.0"
  }
}
